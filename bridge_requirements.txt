# Robot Serial Bridge Requirements
# Run: pip install -r bridge_requirements.txt

# Core ROS 2 dependencies
rclpy>=3.0.0

# Standard Python packages for ROS message types
geometry-msgs>=4.0.0
sensor-msgs>=4.0.0
nav-msgs>=4.0.0
std-msgs>=4.0.0

# Serial communication
pyserial>=3.5

# Direct RPi hardware mode (I2C + GPIO)
smbus2>=0.4.3
RPi.GPIO>=0.7.1; platform_machine == "armv7l" or platform_machine == "aarch64"

# Optional: For enhanced logging and debugging
colorlog>=6.0.0
