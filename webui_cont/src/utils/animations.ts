import anime from 'animejs';
export const animateLandingPage = (containerRef: HTMLElement | null, logoRef: HTMLElement | null) => { if(!containerRef) return; const entrySection = containerRef.querySelector('.entry-section'); if (entrySection) { anime({ targets: entrySection as HTMLElement, opacity:[0,1], translateY:[20,0], easing:'easeOutQuad', duration:800 }); } if (logoRef) { anime({ targets: logoRef, opacity:[0,1], translateY:[-20,0], easing:'easeOutExpo', duration:1000, delay:200 }); } };
export const animateAdvancedForm = (formRef: HTMLElement | null, isVisible: boolean) => { if(!formRef) return; if (isVisible) { formRef.classList.add('visible'); anime({ targets: formRef, opacity:[0,1], duration:500, easing:'easeOutQuad' }); } else { anime({ targets: formRef, opacity:0, duration:300, easing:'easeOutQuad', complete: () => formRef.classList.remove('visible') }); } };
export const animateButtonPress = (el: HTMLElement | null) => { if(!el) return; anime({ targets: el, scale:[1,0.95,1], duration:300, easing:'easeInOutQuad' }); };
